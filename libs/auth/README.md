# libs/auth

Shared auth utilities for verifying JWTs. Intended as a lightweight helper library other services can import.

Install `jsonwebtoken` in services that use this helper:

```bash
cd services/auth
npm install jsonwebtoken
```
